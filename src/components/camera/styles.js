import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  camera: {
    width: 50,
    height: 50,
    marginRight: 10
  },
  cameraOut: {
    backgroundColor: "red"
  }
});

export default styles;
