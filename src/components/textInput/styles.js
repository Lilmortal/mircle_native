import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textInputContainer: {
    flexDirection: "row",
    alignItems: "center"
  },
  icon: {},
  textInput: {
    flex: 1
  }
});

export default styles;
