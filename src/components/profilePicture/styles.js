import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  profilePicture: {
    width: 100,
    height: 100,
    borderRadius: 100 / 2,
    borderWidth: 1
  }
});

export default styles;
