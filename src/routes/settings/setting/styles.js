import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  setting: {
    borderWidth: 1,
    borderColor: "gray",
    borderRadius: 10
  },
  title: {
    marginTop: 10,
    marginBottom: 10
  }
});

export default styles;
