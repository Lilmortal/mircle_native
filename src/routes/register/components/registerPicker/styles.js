import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  picker: {
    borderColor: "black",
    borderWidth: 1,
    borderRadius: 10,
    marginTop: 10,
    marginBottom: 10
  }
});

export default styles;
