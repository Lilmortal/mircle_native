import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  name: {
    fontWeight: "bold",
    fontSize: 25,
    color: "white"
  },
  email: {
    fontSize: 10,
    color: "white"
  }
});

export default styles;
